{
  "pluginAlias": "JlrSmartcarPlatform",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "JLR Smartcar",
        "required": true
      },
      "clientId": {
        "title": "Smartcar Client ID",
        "type": "string",
        "required": true,
        "description": "Your Smartcar V3 Client ID (starts with client_01…). Find it at dashboard.smartcar.com → API credentials."
      },
      "clientSecret": {
        "title": "Smartcar Client Secret",
        "type": "string",
        "required": true,
        "description": "Your Smartcar Client Secret."
      },
      "userId": {
        "title": "Smartcar User ID",
        "type": "string",
        "required": true,
        "description": "Your Smartcar User ID. Find it at dashboard.smartcar.com → Connections. Copy the User ID shown for your account."
      },
      "pollIntervalSeconds": {
        "title": "Poll Interval (seconds)",
        "type": "integer",
        "default": 60,
        "minimum": 30,
        "description": "How often to refresh vehicle data (minimum 30 s)."
      },
      "notifyWebhookUrl": {
        "title": "Notification Webhook URL (optional)",
        "type": "string",
        "description": "Optional: POST webhook called when vehicle state changes."
      }
    }
  },
  "form": null,
  "display": null
}
